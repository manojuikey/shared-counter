package com.cluster.counter;

/**
 * The Enum Operation Type for counter
 */
public enum OpType {

	/** The increment and get. */
	INCREMENT_AND_GET, 
	/** The decrement and get. */
	DECREMENT_AND_GET;
}